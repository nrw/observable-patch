{
  "name": "observable-patch",
  "description": "A computed observ the computes a patch between a base state and a patched state.",
  "version": "1.0.2",
  "repository": {
    "url": "https://github.com/nrw/observable-patch.git"
  },
  "main": "index.js",
  "scripts": {
    "test": "tape test/*.js"
  },
  "dependencies": {
    "observ": "^0.2.0",
    "patcher": "0.0.6"
  },
  "devDependencies": {
    "tape": "3.x.x"
  }
}
